# タスクリスト

- [x] サポート元画像の整理
  - [x] `public/assets/supports_src/` ディレクトリの作成
  - [x] 古いセッションパスからサポート元画像をコピーし、リネーム (`anemo.png` 等)
  - [x] `scripts/extract_supports.py` のパス指定を相対パスに修正
  - [x] `python scripts/extract_supports.py` を実行してスプライト切り出しが成功することを確認
- [x] 背景画像のAI生成と配置
  - [x] タイトル画面背景 (`title_bg.png`) を生成し、`public/assets/art/` に配置
  - [x] 育成画面背景 (`train_bg.png`) を生成し、`public/assets/art/` に配置
  - [x] 戦闘画面の空背景 (`battle_sky.png`) を生成し、`public/assets/art/` に配置
  - [x] 戦闘画面の遠景 (`battle_back_hills.png`) を生成し、`public/assets/art/` に配置
  - [x] 戦闘画面の中景 (`battle_fore_hills.png`) を生成し、`public/assets/art/` に配置
- [x] 拠点画像の再生成となじませ (追加フィードバック)
  - [x] 2Dカートゥーン/フラットベクター調の味方城画像 (`castle_ally.png`) を生成
  - [x] 2Dカートゥーン/フラットベクター調の敵城画像 (`castle_enemy.png`) を生成
  - [x] 影を含めて綺麗に背景透過処理（白・グレー抜き）を実行して配置
- [x] 勝利条件の変更 (追加フィードバック)
  - [x] `public/js/game.js` の `tendPlot` メソッドから、花畑全開花による勝利処理を削除
- [x] ゲーム終了後の画面揺れの廃止 (追加フィードバック2)
  - [x] `public/js/game.js` の `end` メソッドで `this.shake = 0` にリセット
  - [x] `public/js/game.js` の `draw` メソッドの `shake` 適用条件に状態チェックを追加
- [x] CSS / JS コードの修正と適用
  - [x] `public/css/style.css` を修正し、タイトル背景を適用
  - [x] `public/css/train.css` を修正し、育成背景を適用
  - [x] `public/js/game.js` を修正し、戦闘背景のパララックス描画、拠点の画像描画を実装
  - [x] `public/js/entities.js` で新しい画像リソースのプリロードを実装
- [x] 動作検証と完了報告
  - [x] ローカル開発サーバーでの動作確認
  - [x] `walkthrough.md` の作成
